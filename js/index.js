const path = require('path');
const I = require('immutable');
const data = require('./data');

consdata.generateAssignments().toKeyedSeq()).sortBy(p => I.List(p).get(0)).forEach((p) => {
  let user = I.List(p).get(0);
  let timetable = I.List(p).get(1);
});
